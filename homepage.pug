doctype html
html
  head
    title= title
  body
    h1= message
    if contacts
      ul
        each contact in contacts.results
          li= contact.properties.firstname.value + ' ' + contact.properties.lastname.value
